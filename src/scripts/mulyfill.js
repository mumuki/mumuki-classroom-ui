window.mumuki = {};

mumuki.exercise = {
};

mumuki.CustomEditor = {
  addSource(source) {
    // nothing
  }
};

mumuki.kids = {
  registerStateScaler(scaler) {
    // nothing
  },

  registerBlocksAreaScaler(scaler) {
    // nothing
  }
};

mumuki.assetsLoadedFor = (_) => {};
